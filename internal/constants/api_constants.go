package constants

const ServiceComponent = "awesome-service"
const MB = 1024 * 1024

// API paths
const (
	APIBasePath string = "/api"
)

// API Constants
const (
	CorrelationId string = "correlation_id"
	Authorisation string = "Authorization"
)
